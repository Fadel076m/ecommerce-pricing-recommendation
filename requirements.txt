# Data engineering
pandas>=2.2
openpyxl>=3.1        # lecture online_retail_II.xlsx (UCI)
pyspark>=3.5
duckdb>=1.0
pyarrow>=16.0
boto3>=1.34          # client S3-compatible pour Cloudflare R2
sqlalchemy>=2.0
psycopg2-binary>=2.9

# Data quality : assertions Pytest (great_expectations écarté, voir AGENTS.md
# alternative explicite §2 ; son import déclenche un sys.exit() silencieux
# quand une dépendance optionnelle grpc/protobuf manque — cf. .claude/memory/learnings.md)
pytest>=8.0
httpx>=0.27        # requis par fastapi.testclient.TestClient

# Data science
scikit-learn>=1.4
prophet>=1.1
lightgbm>=4.3
faiss-cpu>=1.8

# MLOps
mlflow>=2.12

# API & dashboard
fastapi>=0.111
uvicorn[standard]>=0.30
dash>=2.17
plotly>=5.22

# Orchestration (optionnel selon jalon 10) : voir requirements-airflow.txt,
# à installer séparément avec le fichier de contraintes officiel (sinon le
# resolver pip peut rester bloqué indéfiniment sur cet arbre de dépendances).

# Utilitaires
python-dotenv>=1.0
pydantic>=2.7
requests>=2.31       # dashboard -> API (AGENTS.md §5 : le dashboard ne recalcule rien)
