---
type: journal
project: ecommerce-pricing-recommendation
---

# Journal de session

## 2026-08-18

Setup initial du repo à partir des deux briefs (ISM + Gest Projet) et de l'audit réel des données dans `Projet Ecommerce/data`. Structure de dossiers créée, `AGENTS.md`/`CLAUDE.md` en place, roadmap daté sur 6 jours (deadline dimanche 23/08). Intégration du système de mémoire Claude Code (`.claude/memory/`, `memory.sh`) et des conventions d'environnement depuis l'espace Notion de l'utilisateur (accès accordé en cours de session). Prochaine étape : démarrer le Jalon 1 (cadrage) mercredi 19/08.

## 2026-08-19

J0 finalisé puis Jalon 1 (Cadrage) terminé. Côté infra : `.env` local créé, Postgres + MLflow up via Docker (`docker-compose.yml` nettoyé), remote GitHub `origin` ajouté et `master` poussé. L'installation Python a buté sur un `pip` bloqué plusieurs heures par `apache-airflow` non contraint (BLK-002/LRN-002) — Airflow isolé dans `requirements-airflow.txt` (BDR-004), réinstallation du cœur relancée. Côté cadrage : `docs/cahier_des_charges.md` complété (objectifs business/techniques repris du brief ISM, personas, use cases dérivés du scénario de démo, KPIs inlinés, critère de validation en une phrase, risk register), `docs/business_case.md` structuré en première version (observé/prédit/simulé + hypothèses de simulation), `docs/rgpd.md` complété avec la politique de conservation. Commit `b1f5e79` poussé sur GitHub (repo créé par l'utilisateur en cours de session : https://github.com/Fadel076m/ecommerce-pricing-recommendation).

Jalon 2 (Dataset) terminé dans la foulée. Venv recréé en Python 3.12 (le 3.14 par défaut faisait exploser les temps de build de pyspark/prophet/lightgbm/faiss-cpu, BDR-005) ; `great_expectations` abandonné après un `sys.exit()` silencieux à l'import sur dépendance optionnelle manquante (BDR-006/LRN-005), data quality en Pytest uniquement (AGENTS.md l'autorise explicitement). `docs/data_dictionary.md` rempli pour les 7 tables du modèle en étoile (statuts observé/dérivé/synthétique explicites, note sur les espaces d'identifiants UCI vs RetailRocket jamais fusionnés). `scripts/data_generator.py` rendu opérationnel : charge `data/raw_local/online_retail_II.xlsx` (copié depuis `Projet Ecommerce/data`, non versionné), nettoie (1 067 371 → 779 495 lignes), génère cost_price/stock/promotion (seed=42) et exporte un échantillon dans `data/sample/` (103 044 lignes fact_sales, 4 160 produits). 9 tests Pytest passent (`tests/test_data_quality.py`), dont 3 d'intégration sur l'échantillon réel.

Jalon 3 (Data Lake R2) + Jalon 4 (Data Warehouse) terminés le même jour (08-19), en avance sur la roadmap qui les prévoyait le 20/08 — journée J2 déjà bouclée. Refactor préalable : logique de construction du modèle en étoile extraite de `scripts/data_generator.py` vers `src/transformation/star_schema.py` (module partagé, réutilisé par le Jalon 2 et le pipeline complet), ajout de `build_dim_customer`/`build_dim_date`, `fact_inventory` devient un instantané avec `date_id` (BDR-007), `dim_promotion` gagne `start_date`/`end_date`. Nouveau module `src/transformation/web_events.py` : traite `events.csv` RetailRocket (2 756 101 lignes, DuckDB) en `fact_web_events`, découpage de session par inactivité 30 min ; `transactionid` s'est révélé non unique par ligne (LRN-007), `event_id` généré par position plutôt que par colonne métier. L'utilisateur a créé le compte/bucket Cloudflare R2 et rempli `.env` lui-même (jamais affiché ni committé) ; `src/ingestion/upload_to_r2.py` upload `raw/orders` + `processed/{customers,products,dates,promotions,sales,inventory,web_events}` (8 objets, ~110 Mo) — a buté sur des colonnes `object` à types mixtes dans le xlsx UCI cassant `to_parquet` (LRN-006). `src/ingestion/verify_r2.py` confirme lecture/analyse DuckDB depuis R2 : le secret natif `TYPE r2` renvoie 404, le secret générique `TYPE s3` avec `ENDPOINT`+`URL_STYLE path` fonctionne (BDR-008). DDL du modèle en étoile dans `data/schemas/ddl.sql` (contraintes CHECK simples, pas de CHECK d'égalité flottante sur revenue/margin/closing_stock — vérifié par les tests à la place) ; `src/transformation/load_to_postgres.py` charge les 7 tables (779 495 fact_sales, 2 756 101 fact_web_events). Vérifié en SQL : 36 975 commandes, CA 16 973 707,88, marge 3 499 564,12, panier moyen 459,06, requête de risque de rupture (jours de stock estimés) fonctionnelle — cohérent avec R2. 16 tests Pytest passent (`tests/test_transformation.py` ajouté, 7 nouveaux tests sur le dataset complet).

Jalon 5 (Forecasting) terminé le même jour, toujours en avance sur la roadmap (prévu vendredi 21/08). Scope : comparaison Baseline Moving Average / Prophet sur la demande quotidienne **agrégée** (Prophet ne gère qu'une série à la fois), + un modèle LightGBM **global** (panel produit×jour, `product_id` en feature catégorielle, 2961 produits retenus avec ≥30 jours d'historique) pour servir `/forecast/{product_id}` pour n'importe quel produit sans entraîner 4600 modèles (BDR-009). Split temporel strict (30 derniers jours en test), testé (`tests/test_forecasting.py`, pas de fuite du futur). Résultats loggés dans MLflow (expérience `forecasting`) : Prophet bat la baseline sur l'agrégé (MAE 8066 vs 10540, RMSE 15344 vs 16663) — critère de validation du jalon rempli. Le MAPE explose sur les trois modèles (314 327 % / 85 553 % / 296 %) car la demande quotidienne passe souvent proche de 0, exactement l'avertissement de la roadmap — MAE/RMSE retenus comme métriques de référence, documenté dans `docs/forecasting.md`. Accroc rencontré : MLflow plante sur console Windows (cp1252) à cause d'emojis imprimés sur stdout, même après un run réussi (LRN-008) — fix générique (`sys.stdout.reconfigure(encoding="utf-8")`) à réutiliser pour les Jalons 6/7. `.gitignore` durci pour `models/` (n'excluait que `*.pkl`/`*.joblib`, ratait le `.txt` du booster LightGBM). 23 tests Pytest passent au total.

Jalon 6 (Pricing) terminé le même jour. `src/pricing/` : elasticity.py (régression log-log par produit, éligibilité ≥20 obs/≥5 prix distincts/CV>5%, fallback assumé -1,5 documenté sinon, bornage [-10,0] après avoir constaté des élasticités positives en sortie brute), simulate.py (grille de 13 prix -30%/+30%, demande à élasticité constante, marge = (prix-cost_price)×demande, argmax), train.py (orchestration + MLflow, expérience `pricing`, résultats persistés dans `models/pricing_recommendations.parquet` pour l'API du Jalon 8). Audit préalable en SQL direct : seuls 1057/4631 produits ont assez de variation de prix pour une estimation crédible (confirmé ensuite par le code : 1057 estimés, 3574 sur fallback). Bug rencontré : un prix à 0 (article offert, valide selon data_quality.md) fait planter `np.polyfit` en `log(0)` (LRN-009) — filtré en amont. R² médian des produits estimés très faible (0,12), documenté sans détour dans `docs/pricing.md` avec toutes les réserves (77% du catalogue en fallback, uplift de marge simulé sur cost_price synthétique — jamais un gain réel). 11 tests Pytest dans `tests/test_pricing.py` (dont un qui vérifie le bornage sur bruit pur). 34 tests Pytest passent au total.

Jalon 7 (Recommendation) terminé le même jour — les trois moteurs ML bouclés en une seule journée (J1 prévu vendredi 21/08). Décision structurante : le moteur est construit entièrement dans l'espace RetailRocket (`visitor_id`/`item_id`), pas UCI (`customer_id`) — c'est la seule source avec le signal view/add_to_cart/purchase demandé par la roadmap, et fusionner les deux espaces aurait violé la règle du brief sur le mélange de sources (BDR-011). Documenté dans `docs/recommendation.md` et `docs/api.md` : `/recommendations/{customer_id}` (Jalon 8) doit être compris comme prenant un `visitor_id` RetailRocket. Filtrage à ≥5 interactions par visiteur/item (897k interactions retenues sur 2,76M, 81 318 visiteurs, 67 625 items), split temporel global à 80% (coupure 14/08/2015), échantillon de 5000 visiteurs évalués (scope, seed=42). Quatre approches : Most Popular (baseline), content-based (catégorie RetailRocket, `item_properties_part1/2.csv` filtré en DuckDB sur les items retenus — jamais pandas brut), collaborative filtering (TruncatedSVD scikit-learn + FAISS, stack imposée), hybride (Reciprocal Rank Fusion). Deux bugs trouvés et corrigés : (1) le content-based ne scorait que les items déjà interagis, perdant tout intérêt cold-start (LRN-010) ; (2) les métriques étaient calculées contre le test set complet (22211 visiteurs) au lieu du seul échantillon recommandé (5000), diluant les scores ~5x sans erreur visible (LRN-011). Résultats finaux : les trois approches battent la baseline (content-based en tête, Precision@10 ×5,6 vs baseline ; collaborative le plus faible sur ce dataset très sparse). Valeurs absolues très basses (~0,001-0,006), attendu vu le catalogue (67k items) — documenté comme "comparaison relative, pas valeur absolue" dans `docs/recommendation.md`, même logique que l'avertissement MAPE du forecasting. 11 tests Pytest (`tests/test_recommendation.py`). 45 tests Pytest passent au total sur le projet. Les trois moteurs ML (Jalons 5-7) sont maintenant tous livrés, testés, documentés et loggés dans MLflow.

Jalon 8 (API FastAPI) terminé le même jour — quatrième jalon bouclé dans la journée. Avant l'API : deux artefacts manquants comblés en repassant les scripts d'entraînement (table de correspondance visitor_id -> recommandations hybrides pour `recommendation.py`, modèle LightGBM de production ré-entraîné sur train+test + liste ordonnée des catégories produit persistée pour `forecasting.py`, cost_price/avg_daily_demand ajoutés à la table pricing pour `POST /pricing/simulate`). `api/` : db.py (connexion Postgres partagée), schemas.py (Pydantic, disclaimers systématiques "sous hypothèses du modèle"), services.py (Forecasting/Pricing/Recommendation, ne recalcule rien qui existe déjà dans src/), main.py (5 routes + lifespan chargeant les artefacts une fois au démarrage). `src/forecasting/predict.py` : prévision récursive J+1..J+7 (le J+1 prédit devient un lag pour J+2, etc., assumé et documenté), fallback baseline si historique <29 jours. Bug évité avant qu'il ne se produise : l'encodage catégoriel LightGBM dépend de l'ordre exact des catégories d'entraînement, jamais recalculé indépendamment (LRN-012). 9 tests Pytest (`tests/test_api.py`, TestClient FastAPI) + vérification manuelle complète en live (curl sur les 5 routes + Swagger `/docs` + `/openapi.json`, tous 200). Dockerfile API : `requirements-api.txt` dédié créé (l'API n'a besoin ni de pyspark/prophet/dash/scikit-learn/faiss/duckdb/boto3) après un premier build cassé par un timeout réseau sur le `requirements.txt` complet — même en allégé, deux timeouts supplémentaires ont forcé l'ajout de `--timeout 100 --retries 10` à pip (LRN-014). Un `OSError: libgomp.so.1` a suivi au premier démarrage du conteneur : `python:3.11-slim` n'a pas la lib OpenMP dont lightgbm dépend, corrigé par `apt-get install libgomp1` (LRN-013). `docker-compose.yml` corrigé : `POSTGRES_HOST`/`MLFLOW_TRACKING_URI` surchargés pour le réseau interne docker-compose (`.env` est prévu pour un usage local hors conteneur), volume `./models:/app/models:ro` ajouté. Conteneur testé de bout en bout via `docker compose up api` : les 5 routes répondent correctement à travers le port exposé. 54 tests Pytest passent au total sur le projet. Prochaine étape : Jalon 9 (Dashboard Dash), toujours en avance sur la roadmap (prévu samedi 22/08).
